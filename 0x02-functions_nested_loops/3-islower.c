#include "main.h"

/**
 * _islower -function that checks for lowercase character
 * @parameterc -variable declaration type int
 *
 * Return: On success 1.
 * Otherwise 0 is returned
 */

int _islower(int c)
{
	int lch;

	lch = _islower(c);
	return (write(1, &c, 1));
}
